package vhss.playback
{
    import flash.utils.*;

    public class SessionPlaybackTimer extends Timer
    {
        public var audio_id:String;

        public function SessionPlaybackTimer(param1:Number, param2:int = 0)
        {
            super(param1, param2);
            return;
        }// end function

    }
}
